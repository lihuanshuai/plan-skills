---
name: write-project-plan
description: 基于真实仓库证据编写可执行、可验证的实施计划。用于用户要求编码前设计、需求拆解或 TDD 计划时。
---

# 编写项目实施计划

约定 `<skill_dir>` 为当前技能目录，`<repo_dir>` 为目标仓库根目录。

## 流程

1. 建立事实：读取需求、`<repo_dir>/AGENTS.md`、相关实现、测试、配置和依赖调用方。不得只根据需求文本推测仓库结构或接口。
2. 确定边界：先列出受影响的项目、文件职责、接口和依赖顺序。多个可独立交付的子系统拆成多份计划；跨仓或 RPC 任务在每项中标明项目、兼容顺序和升级节点。
3. 处理不确定性：优先从代码和文档消除，不向用户询问可由仓库事实回答的问题。低风险且不阻塞的推断作为假设写入计划；按“决策提问”处理会实质改变外部契约、数据模型、范围、验证成本或执行顺序的未决选择。
4. 写入计划：读取 `<skill_dir>/references/plan-document-structure.md`，按其唯一格式写入用户指定路径；未指定时使用 `<repo_dir>/docs/agent_plans/YYYY-MM-DD-<feature-name>.md`。
5. 组织任务：按独立交付边界和依赖关系决定任务数量，提供对应总览和可独立验证的编号任务。用户要求 TDD 或先写测试能明确行为契约时采用失败测试、实现、通过测试的步骤；其他任务选择能证明完成条件的验证，保留必要回归测试和仓库必跑项。仅在新变更、失败或未解决风险需要时扩大验证；提交步骤按交付要求安排。
6. 自审并修复：逐条映射需求到任务，核对路径、符号、签名、依赖顺序、命令和预期结果，扫描占位符与前后矛盾；按“计划语言”检查标题、表头和步骤是否能直接理解。
7. 交付计划路径、关键决策和仍待用户决定的阻塞项；建议后续使用 `execute-project-plan`。本技能只写计划，不实施业务改动。

## 决策提问

只询问无法从现有证据确定且答案会改变计划的问题。优先检查：

- **目标与验收：** 用户可观察行为、明确不做的范围、权限规则、成功指标和验收主体是否确定。
- **架构与归属：** 功能由哪个项目或组件负责；关键数据使用 MySQL、KV、对象存储或其他已有设施；新建还是复用现有存储或服务。
- **多项目证据：** 盘点所有受影响项目及本地工作树。若必要项目不在本地，先判断已有 IDL、生成代码、依赖声明或文档能否证明其契约；证据不足且会影响职责、接口或验证步骤时，请用户提供本地路径、补充或授权获取仓库，或明确只规划当前可见项目。不得凭缺失项目的假设写完整跨项目方案。
- **数据与迁移：** 新建表、修改现有表或增加列；数据量、唯一性、一致性与保留期限；是否需要历史回填、双写、兼容读取、清理和回滚。
- **接口与兼容：** API、RPC、消息或任务契约由谁定义；是否兼容旧客户端、旧数据和灰度期间的新旧版本；依赖升级与切换顺序。
- **非功能约束：** 只有会影响方案时，询问延迟、吞吐、可用性、并发冲突、隐私、安全、审计和成本边界。
- **验证与交付：** 是否要求本地单元测试，以及是否还需 Docker、集成环境、Pre、人工验收、灰度、监控和回滚演练；仓库已规定的必跑项直接纳入计划，只询问额外验证、环境限制或发布边界。

每个问题先给出已知事实与证据，再列出可行选项及其影响，明确推荐方案和理由，最后给出最小回复方式，例如“选择 A/B”或“确认采用推荐方案”。优先一次提出 1–3 个当前阻塞问题；有依赖关系时分轮询问。不要把实现细节选择转嫁给用户，不用开放式问题索取可通过仓库调查获得的信息，也不要让非阻塞问题中断计划；后者记录为显式假设并继续。

## 计划语言

- 面向了解基本开发知识、但不熟悉本项目的读者，使用自然、具体的中文。任务标题直接写“对什么做什么”，步骤说明修改对象、动作与预期结果，不自造术语或堆叠抽象名词。
- 不用“闭环、收口、勾稽、门禁、矩阵、版本原语”等词代替具体说明。例如“验证闭环”写成“运行哪些检查、通过标准是什么”；“清单勾稽”写成“逐项核对清单，列出遗漏或重复项”；“旧入口收口”写明要替换或删除的入口及完成条件。按实际含义改写，不机械替换同义词。
- 能用常见中文表达时，不夹用 inventory、gate、blocker 等英文。必要的技术术语或缩写首次出现时简短解释它在本计划中的含义；保留准确的代码符号、配置字段、命令、协议名和工具要求的状态值，不为简化语言改写它们。
- 直接写清前置条件、失败原因和恢复方式，不用口号代替可执行步骤。简化措辞时保留证据、兼容要求、验证标准和授权边界；不额外堆砌与当前任务无关的术语表。

## 输出契约

- 计划必须让零项目上下文的工程师仅凭文件内容即可执行，不依赖当前对话中的隐含信息。
- 每个任务必须包含状态、客观完成标志、精确文件与符号、按序步骤、验证命令和可观察的预期结果。
- 代码步骤默认写清修改位置、接口契约、关键算法和验收条件；容易歧义的细节或用户明确要求的部分提供完整代码。不用 `TBD`、`TODO`、“同上”或“添加适当处理”等占位表达掩盖未决问题；必要的未决选择写清影响和解决条件。
- 仓库事实、现有输出和外部状态必须有证据；未来步骤的预期结果应明确标为预期，不得伪装成已验证事实。

## 硬约束

- 遵循现有架构、代码风格和仓库命令；不要为满足模板编造文件、API、测试或发布步骤。
- 计划可包含提交、推送和 PR 步骤，但不得把需要用户或仓库负责人执行的 merge 写成 Agent 可自行完成的动作。
- 除计划文件及其目录外不修改代码、配置或运行时状态；除非用户另行要求，不提交或推送计划。
